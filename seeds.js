var mongoose = require('mongoose'); 
    mongoose.connect('mongodb://localhost:27017/fitgeneration');

    var User = require('.models/User');

    var newUsers; 

    newUsers = [
    {
      email: rache@rache.com
      password: rache

    ]
  }
